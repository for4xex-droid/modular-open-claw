[advisories]
# =====================================================================
# RUSTSEC Ignores (Chesterton's Fence)
# =====================================================================
# Issue C / OP-032 CLEARED (2026-07-22):
# extism 1.30 → wasmtime/wasi-common 43.x。未使用の workspace wasmtime /
# wasmtime-wasi 直依存を削除し 41.x と wasi-44（0188）経路を消滅。
# 削除済 ID: 0085–0089, 0091–0096, 0114, 0188（deny と同期）。
ignore = [
    # -----------------------------------------------------------------
    # Serenity (tokio-tungstenite v0.21.0 -> rustls v0.22) & Legacy CVEs:
    # [BLOCKED_BY_UPSTREAM]
    # TARGET: serenity (>= 0.13.0)
    # ACTION: Remove RUSTSEC-2026-0098, 0099, 0104, 0049. Unblocks Issue B (bastion-core).
    # -----------------------------------------------------------------
    "RUSTSEC-2026-0098", # rustls-webpki 0.102.x (pinned by serenity -> rustls 0.22)
    "RUSTSEC-2026-0099", # rustls-webpki 0.102.x (pinned by serenity -> rustls 0.22)
    "RUSTSEC-2026-0104", # rustls-webpki 0.102.x (pinned by serenity -> rustls 0.22)
    "RUSTSEC-2026-0097", # rand (legacy crate, pinned by serenity -> rustls 0.22 tree)
    "RUSTSEC-2024-0421", # idna 0.4.0 (pinned by serenity -> reqwest -> hyper-tls -> url)
    "RUSTSEC-2023-0071", # rsa (Marvin Attack, pinned by sqlx-mysql)
    "RUSTSEC-2026-0049", # rustls-webpki 0.102.x CRL matching (pinned by serenity)
    
    # -----------------------------------------------------------------
    # Tauri & UI dependencies:
    # [BLOCKED_BY_UPSTREAM]
    # TARGET: tauri (>= 3.0.0 or upstream GTK4 migration)
    # ACTION: Unblocks Issue D.
    # -----------------------------------------------------------------
    "RUSTSEC-2024-0429", # glib (pinned by tauri -> webkit2gtk -> glib)
    "RUSTSEC-2025-0075", # unic-char-range (pinned by tauri -> unic-ucd-ident)
    "RUSTSEC-2025-0080", # unic-common (pinned by tauri -> unic-ucd-version)
    "RUSTSEC-2025-0081", # unic-char-property (pinned by tauri)
    "RUSTSEC-2025-0098", # unic-ucd-version (pinned by tauri -> unic-ucd-ident)
    "RUSTSEC-2025-0100", # unic-ucd-ident (pinned by tauri -> urlpattern)
    "RUSTSEC-2025-0134", # rustls-pemfile (pinned by reqwest / bastion-core)
    "RUSTSEC-2025-0017", # trust-dns-proto (pinned by bastion-core)
    "RUSTSEC-2024-0370", # proc-macro-error (unmaintained)
    "RUSTSEC-2024-0388", # derivative (unmaintained)
    "RUSTSEC-2024-0436", # paste (unmaintained)
    "RUSTSEC-2025-0057", # fxhash (unmaintained)
    "RUSTSEC-2024-0411", # gdkwayland-sys (unmaintained via tauri)
    "RUSTSEC-2024-0417", # gdkx11 (unmaintained via tauri)
    "RUSTSEC-2024-0414", # gdkx11-sys (unmaintained via tauri)
    "RUSTSEC-2024-0415", # gtk (unmaintained via tauri)
    "RUSTSEC-2024-0420", # gtk-sys (unmaintained via tauri)
    "RUSTSEC-2024-0419", # gtk3-macros (unmaintained via tauri)
    "RUSTSEC-2024-0413", # atk (unmaintained via tauri)
    "RUSTSEC-2024-0416", # atk-sys (unmaintained via tauri)
    "RUSTSEC-2024-0412", # gdk (unmaintained via tauri)
    "RUSTSEC-2024-0418", # gdk-sys (unmaintained via tauri)
    "RUSTSEC-2025-0119", # number_prefix (unmaintained via indicatif)
    "RUSTSEC-2026-0186", # memmap2 (unchecked pointer offset)
    "RUSTSEC-2026-0173", # proc-macro-error2 (unmaintained)
    "RUSTSEC-2026-0190", # anyhow downcast_mut Soundness issue
    "RUSTSEC-2026-0194", # quick-xml quadratic run time duplicate attribute (tauri -> plist)
    "RUSTSEC-2026-0195"  # quick-xml unbounded namespace allocation (tauri -> plist)
]

