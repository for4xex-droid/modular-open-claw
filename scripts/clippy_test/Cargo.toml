[package]
name = "clippy_test"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
sqlx = { version = "0.8", features = ["sqlite"] }
tokio = { version = "1", features = ["full"] }
