# AGENTS.md - Development Agent Rules

このファイルは IDE 上の開発エージェント（コーディング AI）のための絶対的な行動規範です。

## 行動規範

1. **敬語厳守**: 常に「です・ます調」で応答する。タメ口（「すまん」「やるぞ」等）は厳禁。
2. **ユーザー意思最優先**: 「計画を立てろ」→ 計画を立てる。「実行しろ」→ 実行する。勝手に判断しない。
3. **ツール呼び出し品質**: ディレクトリを `view_file` に渡さない。存在確認なしにファイルを参照しない。
4. **一応答最大進捗**: 承認された計画に基づき、可能な限り多くの変更を一度にまとめる。
5. **冗長性の排除**: 「承知しました」の連発、繰り返し説明、形式的な前置きは不要。
   ドキュメント・コミットメッセージ・エラーメッセージの品質基準:
   - 「各種改善を行いました」→ 具体的な変更内容を記述
   - 「エラーが発生しました」→ 原因と対処法を含める
   - `AiomeError` のメッセージ文字列は、デバッグに十分な文脈を含むこと
   - CHANGELOG エントリはユーザーが「何が変わったか」を即座に理解できること
   - RFC/仕様書の丸写し禁止。自分の言葉で要約すること

## Safety

- **秘密情報の開示制限と確認プロセス**:
  - 秘密情報（APIキー、暗号化鍵、秘密鍵、パスワード等）を対話やファイルに書き出す際、ユーザーが明確に変数の名称や範囲を特定しない限り、他の無関係な機密鍵を無差別に出力（過剰開示）してはならない。
  - 要求が曖昧な場合（例: 「シークレットキーを出せ」等）は、必ず「どのコンポーネントまたは変数のキーを求めていますか？」と確認を挟み、承認を得た対象のみを開示する。
- 秘密情報を外部に漏洩しない。
- 破壊的コマンド（rm, drop 等）は必ず確認を取る。`trash` > `rm`。
- 不確実な操作は事前に確認する。

## 📚 Documentation Sync Rule

実装タスクを完了した際は、以下のチェックを必ず実行せよ：

1. **CHANGELOG.md** — [Unreleased] に追記されているか？
2. **README_en.md** — README.md 変更時、英語版も同期されているか？
3. **.env.example** — 環境変数追加/変更時、テンプレート更新済みか？
4. **/docs-sync** — 必要に応じて `/docs-sync` ワークフローを実行。
5. **RIPPLE_MAP.md** — 新規ファイル/構造体追加時、`.context/RIPPLE_MAP.md` に影響範囲を追記。
6. **ADR** — 重要な設計判断は `docs/decisions/` に記録。
7. **cargo test** — `cargo check --workspace --tests && cargo test --workspace` が PASS するか？
8. **Golden Rules** — `.agent/skills/docs-ui-ux-golden-rules.md` を遵守。
9. **Mission Control** — 大規模変更前に `.agent/skills/mission-control-principles.md` の4原則を実行。
10. **DESIGN.md** — `tokens.css` / `animations.css` 変更時、DESIGN.md も同期。
11. **SYNERGY.md** — トレイト/クレート/MCP ツール追加・変更時、`docs/architecture/AIOME_NURTURE_SYNERGY.md` の該当セクション（クラス図・シーケンス図・依存マップ）も同期。
12. **tauri.conf.json** — CSP / externalBin 変更時、`.agent/skills/tauri-development.md` の T-003 を確認。

## 🚨 Impact Analysis (影響範囲の特定)

本番コード変更時は事前に影響範囲（被害半径）を特定すること。
廃止された AST パーサ（nurture_auditor.py 等）への依存は禁止されています。

1. `grep_search` ツールを用いて、変更対象のシンボル（構造体・関数・モジュール）の `use` 参照箇所を特定する。
2. `docs/architecture/ARCHITECTURE.md` で最新のコンポーネント依存関係を確認する。
3. `.context/RIPPLE_MAP.md` で意味的依存関係を確認する。

記憶や推測のみに頼った変更は厳禁。

## 🛡️ Code Consistency

Rust 警告対処は Preserve Intent 原則。未使用コードは安易に削除せず `#![allow(...)]` で抑制。

## 🎯 Goal-Driven Execution

命令的なタスク指示を、検証可能な成功基準に変換すること。

| ユーザーの指示 | 変換後のゴール |
|---|---|
| 「バリデーション追加」 | 「不正入力のテストを書き、パスさせる」 |
| 「バグ修正」 | 「再現テストを書き、パスさせる」 |
| 「リファクタ」 | 「既存テストが変更前後で全パスすることを確認」 |

マルチステップタスクでは検証ポイントを明示：
1. [ステップ] → 検証: [確認方法]
2. [ステップ] → 検証: [確認方法]

明確な成功基準があれば自律ループで完遂せよ。曖昧な基準はユーザーに確認を求めよ。

## 🔴 Verification Protocol（絶対遵守）

「動くか検証しろ」と指示された場合、以下の3段階を**必ず**実行すること。
「実行して成功した」は**検証ではない**。

### Step 1: Positive Test（正常系）
ツール/機能を実行し、期待通りの出力が得られることを確認する。

### Step 2: Negative Test（異常注入 — 必須）
**意図的に障害を注入し、ツールがそれを正しく検知・拒否することを確認する。**
- セキュリティツール → 脆弱性を仕込んで検知されるか
- バリデーション → 不正入力を投げて拒否されるか
- テストスイート → 意図的にコードを壊してテストが落ちるか

> **このステップを省略することは、いかなる理由があっても許されない。**
> exit code 0 は「ツールが正しい」ことの証明にはならない。
> 「エラーが出ない」と「正しく動いている」は別の命題である。

### Step 3: Revert & Report
注入した障害を元に戻し、正常状態に復帰したことを確認する。
3段階すべての結果を報告する。

> 由来: 2026-04-27、taint_scanner.py を exit code 0 のみで「機能している」と3回誤報告した（実態はハリボテ）。Negative Test があれば1回目で発覚していた。

## 🔒 SafeToAutoRun 原則（絶対遵守）

**読み取り専用コマンドは `SafeToAutoRun: true` を必ず設定せよ。**

以下のコマンドはファイルシステムやシステム状態を一切変更しないため、常に自動実行可能：
- `grep`, `rg`, `find`, `ls`, `cat`, `head`, `tail`, `wc`
- `cargo check`, `cargo test`（ビルドのみ、成果物の書き込みは一時ディレクトリ）
- `python3 scripts/impact_query.py`
- `git log`, `git status`, `git diff`, `git show`

**ユーザー承認が必要なコマンド（`SafeToAutoRun: false`）:**
- `git commit`, `git push`, `git restore`, `git checkout`
- `cargo install`, `npm install`
- `rm`, `trash`, `mv`, `cp`（ファイル操作）
- 外部 API 呼び出し、ネットワーク送信

> 由来: 2026-04-29、読み取り専用コマンドが毎回承認待ちとなりスキャンが中断。承認待ちの乱発はユーザー体験を著しく損なう。

## 🎯 ツール呼び出し品質（絶対遵守）

**`grep_search` / `run_command` で検索を行う場合、以下を厳守せよ：**

1. **SearchPath は必ずリポジトリ単位以下**。`/Users/.../antigravity/` 全体を指定してはならない。`/Users/.../aiome/` と `/Users/.../Project-Nurture/` を個別に指定する。
2. **パスの存在を事前に確認**。ディレクトリなのかファイルなのか不明なパスを grep_search に渡さない。`supervisor/` と `supervisor.rs` を間違えたら即エラーでスキャンが停止する。
3. **正規表現パターンは最小限**。`agentic_karma|AgenticKarma|karma_extraction` のような OR 検索は結果が巨大になりやすい。具体的な1パターンに絞る。

> 由来: 2026-04-29、`antigravity/` 全体検索のタイムアウト・存在しないパス指定・並列検索の巻き添えキャンセルが同時発生し、さらに原因を誤診して信頼を損なった。

## 🚫 Scope Lock 原則（絶対遵守）

**ユーザーが指示したフェーズの境界を絶対に越えるな。**

| ユーザーの指示 | 許可された行動 | 禁止された行動 |
|---|---|---|
| 「計画を立てろ」 | 計画書を作成する | コードを変更する |
| 「チェックしろ」「スキャンしろ」 | 読み取り・分析・レポート作成 | コードを変更する |
| 「続けろ」 | **現在のフェーズ内で**作業を続ける | 次のフェーズに勝手に移行する |
| 「実装しろ」「コードを書け」 | コードを変更する | — |

- **「続けろ」≠「次のフェーズに移行しろ」**。現在のスコープの継続のみを意味する。
- フェーズの移行には「実装しろ」「コードを書け」「修正しろ」等の**明示的な許可**が必要。
- 疑わしい場合は「次のフェーズに移ってよろしいでしょうか？」と確認する。

### 行動ゲート（推論バイアス対策）

**「続けろ」を受信したとき、以下を必ず実行すること：**

1. 現在のフェーズを特定する（計画 / スキャン / 実装 / 検証）
2. 「現在は[Xフェーズ]です。[X]の範囲内で継続します」と**応答の冒頭で宣言**する
3. **コード変更ツール（`replace_file_content`, `multi_replace_file_content`, `write_to_file` でプロジェクトコードを対象）を呼び出す前に、必ず「実装に移行してよろしいですか？」と確認する**
4. 確認なしにコード変更ツールを呼び出すことは**絶対に禁止**

> 由来: ルールの文書化だけでは推論時に「文脈の流れ」が優先され無視される。「確認を挟む」という具体的アクションの強制で、スコープ逸脱を物理的に防止する。2026-04-29、「続けろ」を「実装移行」と拡大解釈して無許可でコードを変更するミスが複数回発生した。

## 🔐 Safety-Critical Zone（人間専有コード）

以下のコードパスは、エージェントの自律変更を**禁止**する。
変更にはユーザーの明示的な「実装しろ」「修正しろ」の許可が必須：

1. **認証・認可**: `auth.rs`, `auth_middleware`, `AiomeCustomClaims`
2. **暗号化**: `key-proxy/`, `AbyssVault`, `sovereign_verifier`
3. **経済処理**: `commerce.rs`, `StripeCommerceEngine`, Webhook ハンドラ
4. **データ削除**: `DELETE` エンドポイント、DB マイグレーション of `DROP`
5. **外部送信**: メール送信, Webhook 発火, SNS 投稿の自律実行
6. **CI/CD**: `.github/workflows/`, `pattern-enforcer.sh`
7. **Tauri シェル**: `src-tauri/src/lib.rs`, `tauri.conf.json`, サイドカー起動ロジック

> 由来: rust-lang/rust-forge PR #1040 の原則。安全性に直結するコードは人間が完全に理解した上で変更し、ハルシネーションによる認証バイパス・経済損失リスクを構造的に排除する。

## メモリ管理

- `memory/YYYY-MM-DD.md` は 20 行以内の箇条書き（Done / Open / Lessons の3セクション固定）。
- 冗長な記述禁止。詳細は CHANGELOG.md と RIPPLE_MAP.md に委譲する。
- **未解決タスクの正本は `OPEN.md`**。日次の Open は当日メモにすぎない。タスク完了時・新規発生時は必ず `OPEN.md` を更新し、サイレント消失を防ぐこと。

## 🔑 ローカルログイン手動検証（UIログインテスト）

- ログイン画面は**パスワード欄のみ**（メールアドレス欄は存在しない）。開発環境の `aiome.db` は `SuperSecretPassword123!` で**ログイン可能な状態に設定済み**。そのまま入力してログインすること。
- ログイン可能な状態にもかかわらず DB の再上書き・再生成を計画することは**厳禁**。`admin_password_hash` レコードの**削除も厳禁**（SetupWizard が起動してしまう）。
- DB 初期化等でログインが弾かれた場合のみ、`docs/guides/LOCAL_LOGIN_VERIFICATION.md` の完全手順（バックアップ → Argon2 ハッシュ生成 → 一時上書き → Negative Test → 復元）に従うこと。

